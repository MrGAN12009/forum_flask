# 🚀 Начало работы с проектом

Пошаговое руководство для абсолютных новичков.

## 📋 Что вам понадобится

### Обязательно

✅ **Компьютер с:**
- Windows 10/11, macOS 10.15+, или Linux (Ubuntu 20.04+)
- 4 GB RAM минимум (8 GB рекомендуется)
- 10 GB свободного места на диске

✅ **Установленное ПО:**
- [Docker Desktop](https://www.docker.com/products/docker-desktop) (Windows/Mac)
- или Docker + Docker Compose (Linux)
- Git (опционально)
- Текстовый редактор (VS Code рекомендуется)

### Опционально

- Python 3.11+ (для локальной разработки)
- Postman или подобное (для тестирования API)

## 🎯 Цели этого руководства

После прохождения вы сможете:
1. ✅ Запустить форум локально
2. ✅ Зарегистрировать аккаунт
3. ✅ Создать топик
4. ✅ Использовать чат
5. ✅ Понимать базовую структуру проекта

**Время:** 15-30 минут

---

## Шаг 1: Установка Docker

### Windows

1. Скачайте [Docker Desktop для Windows](https://www.docker.com/products/docker-desktop)
2. Запустите установщик
3. Следуйте инструкциям на экране
4. Перезагрузите компьютер
5. Запустите Docker Desktop
6. Дождитесь запуска (иконка в трее станет зеленой)

**Проверка:**
```powershell
docker --version
docker-compose --version
```

### macOS

1. Скачайте [Docker Desktop для Mac](https://www.docker.com/products/docker-desktop)
2. Откройте .dmg файл
3. Перетащите Docker в папку Applications
4. Запустите Docker из Applications
5. Дождитесь запуска (иконка в верхнем меню)

**Проверка:**
```bash
docker --version
docker-compose --version
```

### Linux (Ubuntu/Debian)

```bash
# Обновите систему
sudo apt update
sudo apt upgrade -y

# Установите Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Добавьте пользователя в группу docker
sudo usermod -aG docker $USER

# Выйдите и войдите снова
exit
```

**Проверка:**
```bash
docker --version
docker compose version
```

---

## Шаг 2: Получение проекта

### Вариант А: Скачать ZIP (проще)

1. Скачайте проект как ZIP
2. Распакуйте в удобное место
3. Откройте папку в терминале

### Вариант Б: Клонировать через Git

```bash
# Установите Git если еще не установлен
# Windows: https://git-scm.com/download/win
# Mac: brew install git
# Linux: sudo apt install git

# Клонируйте репозиторий
git clone <url-репозитория>
cd forum
```

---

## Шаг 3: Создание дефолтного аватара

**Windows PowerShell:**
```powershell
python create_default_avatar.py
```

**Mac/Linux:**
```bash
python3 create_default_avatar.py
```

**Если Python не установлен:**

Скачайте любую картинку 200x200px и сохраните как:
```
app/static/uploads/avatars/default-avatar.png
```

Можно использовать этот URL:
```
https://ui-avatars.com/api/?name=User&size=200&background=0D6EFD&color=fff
```

---

## Шаг 4: Настройка окружения

### Создайте файл .env

**Windows PowerShell:**
```powershell
Copy-Item .env.example .env
notepad .env
```

**Mac/Linux:**
```bash
cp .env.example .env
nano .env
```

### Отредактируйте .env

**Минимальная конфигурация (для начала можно оставить как есть):**
```env
FLASK_APP=run.py
FLASK_ENV=development
SECRET_KEY=your-secret-key-change-this
DATABASE_URL=postgresql://postgres:postgres@db:5432/forum_db
UPLOAD_FOLDER=/app/uploads
MAX_CONTENT_LENGTH=16777216
ALLOWED_EXTENSIONS=png,jpg,jpeg,gif
```

**Важно:** В продакшене измените `SECRET_KEY`!

**Генерация секретного ключа:**

**Python:**
```bash
python -c "import secrets; print(secrets.token_hex(32))"
```

Скопируйте результат в `SECRET_KEY=...`

---

## Шаг 5: Запуск Docker контейнеров

### Откройте терминал в папке проекта

**Windows:**
- Shift + правая кнопка мыши → "Открыть PowerShell здесь"
- Или используйте `cd путь\к\проекту`

**Mac/Linux:**
- Откройте Terminal
- `cd путь/к/проекту`

### Запустите Docker Compose

```bash
docker-compose up -d
```

**Что происходит:**
1. 📦 Скачиваются Docker образы (первый раз ~5-10 минут)
2. 🏗️ Собирается образ Flask приложения
3. 🗄️ Создается контейнер PostgreSQL
4. 🌐 Создается контейнер Nginx
5. 🚀 Запускается Flask приложение
6. 📊 Применяются миграции базы данных

**Наблюдайте за логами:**
```bash
docker-compose logs -f
```

Нажмите `Ctrl+C` чтобы выйти из логов (контейнеры продолжат работать).

**Дождитесь сообщений:**
```
web_1    | Running database migrations...
web_1    | Starting application...
```

---

## Шаг 6: Открытие форума

### Откройте браузер

Перейдите на: **http://localhost**

**Что вы должны увидеть:**
- Главная страница форума
- Кнопки "Вход" и "Регистрация"
- Статистика (0 пользователей, 0 топиков)

**Если не открывается:**

1. Проверьте что Docker запущен
2. Проверьте логи: `docker-compose logs web`
3. Проверьте порты: `docker-compose ps`
4. Попробуйте http://127.0.0.1

---

## Шаг 7: Регистрация первого пользователя

1. **Нажмите "Регистрация"**

2. **Заполните форму:**
   - Имя пользователя: `admin` (минимум 3 символа)
   - Email: `admin@example.com`
   - Пароль: `password123` (минимум 6 символов)
   - Подтверждение: `password123`

3. **Нажмите "Зарегистрироваться"**

4. **Вы будете перенаправлены на страницу входа**

5. **Войдите с вашими данными**

✅ **Поздравляем! Вы зарегистрировались!**

---

## Шаг 8: Создание первого топика

1. **В навигации нажмите "Топики"**

2. **Нажмите "Создать топик"** (зеленая кнопка)

3. **Заполните форму:**
   - Заголовок: `Мой первый топик`
   - Содержание: `Это мой первый топик на форуме!`

4. **Нажмите "Создать топик"**

5. **Вы увидите ваш топик!**

✅ **Отлично! Вы создали топик!**

---

## Шаг 9: Добавление поста

1. **На странице топика прокрутите вниз**

2. **В форме "Ваш ответ" напишите:**
   ```
   Это мой первый пост!
   ```

3. **Опционально: прикрепите изображение**

4. **Нажмите "Отправить"**

✅ **Супер! Вы оставили комментарий!**

---

## Шаг 10: Тестирование чата

**Для теста чата нужны 2 пользователя.**

### Создайте второго пользователя

1. **Выйдите** (иконка пользователя → Выход)

2. **Зарегистрируйте нового:**
   - Username: `user2`
   - Email: `user2@example.com`
   - Password: `password123`

3. **Войдите как user2**

### Отправьте сообщение

1. **Нажмите "Чат" в навигации**

2. **Выберите пользователя `admin`**

3. **Напишите сообщение:**
   ```
   Привет!
   ```

4. **Нажмите "Отправить"**

### Проверьте от имени admin

1. **Откройте форум в режиме инкогнито** (Ctrl+Shift+N)

2. **Войдите как admin**

3. **Нажмите "Чат"**

4. **Вы увидите счетчик непрочитанных!**

5. **Откройте чат с user2**

6. **Вы увидите сообщение!**

✅ **Превосходно! WebSocket работает!**

---

## Шаг 11: Редактирование профиля

1. **Нажмите на иконку пользователя**

2. **Выберите "Профиль"**

3. **Нажмите "Редактировать профиль"**

4. **Загрузите аватар:**
   - Выберите изображение (JPG, PNG, GIF)
   - Максимум 16MB
   - Будет автоматически уменьшено до 200x200

5. **Измените имя (опционально)**

6. **Нажмите "Сохранить изменения"**

✅ **Отлично! Профиль обновлен!**

---

## 🎉 Поздравляем!

Вы успешно:
- ✅ Установили Docker
- ✅ Запустили проект
- ✅ Зарегистрировались
- ✅ Создали топик и пост
- ✅ Протестировали чат
- ✅ Обновили профиль

## 📚 Что дальше?

### Для пользователей:
- Создайте больше топиков
- Пригласите друзей
- Загружайте изображения

### Для разработчиков:

1. **Изучите код:**
   ```bash
   code .  # Откроет VS Code
   ```

2. **Прочитайте документацию:**
   - [README.md](README.md) - полная документация
   - [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md) - архитектура
   - [ARCHITECTURE.md](ARCHITECTURE.md) - диаграммы

3. **Попробуйте изменить:**
   - Цвета в `app/static/css/style.css`
   - Тексты в шаблонах `app/templates/`
   - Логику в `app/*/routes.py`

4. **Создайте свою функцию:**
   - Прочитайте [CONTRIBUTING.md](CONTRIBUTING.md)
   - Следуйте примерам кода
   - Протестируйте

### Для DevOps:

1. **Изучите Docker:**
   ```bash
   docker-compose ps
   docker-compose logs
   docker-compose exec web bash
   ```

2. **Прочитайте:**
   - [DEPLOYMENT.md](DEPLOYMENT.md) - развертывание
   - [COMMANDS.md](COMMANDS.md) - все команды

3. **Разверните на сервере:**
   - Следуйте инструкциям в DEPLOYMENT.md
   - Настройте HTTPS
   - Настройте backup

## 🛠️ Полезные команды

### Просмотр логов
```bash
docker-compose logs -f web
```

### Перезапуск
```bash
docker-compose restart
```

### Остановка
```bash
docker-compose down
```

### Запуск заново
```bash
docker-compose up -d
```

### Статус контейнеров
```bash
docker-compose ps
```

### Вход в контейнер
```bash
docker-compose exec web bash
```

### Создание тестовых данных
```bash
docker-compose exec web python scripts/init_db.py
```

## ❓ Частые проблемы

### Порт 80 занят

**Решение:**

В `docker-compose.yml` измените:
```yaml
nginx:
  ports:
    - "8080:80"  # Вместо "80:80"
```

Затем открывайте: http://localhost:8080

### Docker не запускается

**Windows:**
- Проверьте что Hyper-V включен
- Перезапустите Docker Desktop
- Перезагрузите компьютер

**Mac:**
- Дайте Docker разрешения в System Preferences
- Проверьте что достаточно RAM выделено

**Linux:**
- `sudo systemctl start docker`
- `sudo usermod -aG docker $USER`

### База данных не создается

```bash
# Остановите все
docker-compose down

# Удалите volumes
docker-compose down -v

# Запустите заново
docker-compose up -d
```

### Изображения не загружаются

Проверьте права на папку:
```bash
# Linux/Mac
chmod -R 755 uploads/

# Windows
# Проверьте что Docker имеет доступ к диску
```

## 💡 Советы

1. **Используйте Makefile** (если доступен):
   ```bash
   make help    # Все команды
   make up      # Запустить
   make logs    # Логи
   make down    # Остановить
   ```

2. **Добавьте тестовые данные:**
   ```bash
   docker-compose exec web python scripts/init_db.py
   ```
   Создаст пользователей: user1, user2, user3
   Пароль: password123

3. **Откройте DevTools** в браузере (F12):
   - Console - ошибки JavaScript
   - Network - запросы
   - WS - WebSocket соединения

4. **Сохраняйте изменения:**
   ```bash
   git add .
   git commit -m "Описание изменений"
   ```

## 📞 Нужна помощь?

1. **Проверьте логи:**
   ```bash
   docker-compose logs -f
   ```

2. **Прочитайте документацию:**
   - [INDEX.md](INDEX.md) - навигация
   - [README.md](README.md) - полная документация

3. **Задайте вопрос:**
   - Создайте Issue в репозитории
   - Укажите вашу ОС
   - Приложите логи

## 🎯 Следующие шаги

**Новичок в программировании?**
→ Изучите основы Python и Flask

**Знаете Python?**
→ Читайте [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md)

**Хотите развернуть?**
→ Читайте [DEPLOYMENT.md](DEPLOYMENT.md)

**Хотите участвовать?**
→ Читайте [CONTRIBUTING.md](CONTRIBUTING.md)

---

**Добро пожаловать в проект! 🚀**

**Версия:** 1.0.0

**Лицензия:** MIT

